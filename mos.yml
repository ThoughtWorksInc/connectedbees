author: ThoughtWorkss
description: A demo platform for a connected workspace set of sensors
# arch: PLATFORM
version: 1.0
manifest_version: 2018-04-14
libs_version: ${mos.version}
modules_version: ${mos.version}
mongoose_os_version: ${mos.version}

config_schema:
  - ["mqtt.server", "iot.eclipse.org:1883"]
  - ["i2c.enable", true]

tags:
  - js

filesystem:
  - fs

libs:
  - origin: https://github.com/mongoose-os-libs/js-demo-bundle
